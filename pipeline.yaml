apiVersion: v1
kind: List
metadata: {}
items:
- apiVersion: v1
  kind: BuildConfig
  metadata:
    annotations:
      pipeline.alpha.openshift.io/uses: '[{"name": "sample", "namespace": "first-project", "kind": "DeploymentConfig"}]'
    name: sample-service-pipeline
  spec:
    source:
      git:
        ref: pipeline
        uri: git://github.com/sarguna19/SampleWebApplication.git
      type: Git
    strategy:
      type: JenkinsPipeline
      jenkinsPipelineStrategy:
        jenkinsfilePath: Jenkinsfile
    triggers:
    - generic:
        secret: FiArdDBH
      type: Generic
